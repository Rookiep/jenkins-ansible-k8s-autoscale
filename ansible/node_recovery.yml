---
- name: Kubernetes Node Auto-Recovery (Minikube or Cluster)
  hosts: localhost
  connection: local   # 👈 Prevent SSH, use local connection
  gather_facts: yes

  vars:
    kubeconfig_path: "/home/partho/.kube/config"

  tasks:
    - name: Check Kubernetes cluster accessibility
      shell: kubectl cluster-info --kubeconfig {{ kubeconfig_path }}
      register: cluster_status
      ignore_errors: yes
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"

    - name: Abort if Kubernetes cluster not accessible
      fail:
        msg: |
          ❌ Kubernetes cluster is not accessible.
          💡 Run 'minikube start' or check your kubeconfig context.
      when: cluster_status.rc != 0

    - name: Display cluster info if accessible
      debug:
        msg: "✅ Connected to Kubernetes cluster successfully."

    - name: Restart Kubernetes node (Minikube)
      shell: |
        echo "🔄 Restarting Minikube node..."
        minikube stop
        minikube start
      when: cluster_status.rc == 0